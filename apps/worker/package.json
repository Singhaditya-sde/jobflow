{
  "name": "@jobflow/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@jobflow/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "ioredis": "^5.4.1"
  }
}